self.addEventListener('install',()=>self.skipWaiting());
self.addEventListener('activate',e=>e.waitUntil(self.clients.claim()));
self.addEventListener('push',event=>{let d={};try{d=event.data?event.data.json():{}}catch{}const title=d.title||'LoadFlow — Doca liberada';const options={body:d.body||'Dirija-se à doca indicada.',tag:d.tag||'loadflow-call',renotify:true,requireInteraction:true,icon:'./icon-192.png',badge:'./icon-192.png',vibrate:[500,180,500,180,900],data:{url:d.url||'./'}};event.waitUntil(self.registration.showNotification(title,options))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'./';event.waitUntil((async()=>{const wins=await self.clients.matchAll({type:'window',includeUncontrolled:true});for(const c of wins){try{if(new URL(c.url).origin===new URL(url,self.location.href).origin){await c.focus();if('navigate'in c)await c.navigate(url);return}}catch{}}return self.clients.openWindow(url)})())});
